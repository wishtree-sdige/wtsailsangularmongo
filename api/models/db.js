/* sails.config.globals.Mongoose.connect('mongodb://localhost:27017/Shell', { useNewUrlParser: 'true' });

sails.config.globals.Mongoose.connection.on('error', (err) => {
  Logger.error('Mongoose connection error', err);
});

sails.config.globals.Mongoose.connection.on('connected', () => {
  Logger.info('Mongoose is connected');
});

// require('./Admin');
 */
